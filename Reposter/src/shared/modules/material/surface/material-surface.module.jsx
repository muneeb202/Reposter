import * as MaterialUI from '@mui/material';

/**
 * reusable Material Accordion  with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Accordion = (props) => {
    return (
        <MaterialUI.Accordion  {...props} />
    )
}

/**
 * reusable Material AccordionSummary   with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const AccordionSummary  = (props) => {
    return (
        <MaterialUI.AccordionSummary   {...props} />
    )
}

/**
 * reusable Material AccordionDetails    with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const AccordionDetails   = (props) => {
    return (
        <MaterialUI.AccordionDetails    {...props} />
    )
}

/**
 * reusable Material AppBar with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const AppBar = (props) => {
    return (
        <MaterialUI.AppBar {...props} />
    )
}

/**
 * reusable Material Card  with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Card = (props) => {
    return (
        <MaterialUI.Card  {...props} />
    )
}
/**
 * reusable Material CardMedia  with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const CardMedia = (props) => {
    return (
        <MaterialUI.CardMedia  {...props} />
    )
}
/**
 * reusable Material CardHeader with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const CardHeader= (props) => {
    return (
        <MaterialUI.CardHeader {...props} />
    )
}
/**
 * reusable Material CardContent  with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const CardContent = (props) => {
    return (
        <MaterialUI.CardContent  {...props} />
    )
}

/**
 * reusable Material Paper  with just export with diffrent style
 * @param {*} props 
 * @returns 
 */
export const Paper = (props) => {
    return (
        <MaterialUI.Paper  {...props} />
    )
}